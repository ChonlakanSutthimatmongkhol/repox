import '../../data/models/{{.SnakeName}}_request.dart';
import '../../data/models/{{.SnakeName}}_response.dart';
import '../repositories/{{.SnakeName}}_repository.dart';

class {{.PascalName}}{{.UsecaseSuffix}} {
  const {{.PascalName}}{{.UsecaseSuffix}}(this._repository);

  final {{.PascalName}}{{.RepositorySuffix}} _repository;

  Future<{{.PascalName}}Response> call({{.PascalName}}Request request) {
    return _repository.fetch(request);
  }
}
