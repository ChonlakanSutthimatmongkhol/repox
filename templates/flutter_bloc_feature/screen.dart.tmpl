import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

import '{{.BlocImport}}';

class {{.PascalName}}{{.ScreenSuffix}} extends StatefulWidget {
  const {{.PascalName}}{{.ScreenSuffix}}({super.key});

  @override
  State<{{.PascalName}}{{.ScreenSuffix}}> createState() => _{{.PascalName}}{{.ScreenSuffix}}State();
}

class _{{.PascalName}}{{.ScreenSuffix}}State extends State<{{.PascalName}}{{.ScreenSuffix}}> {
  @override
  void initState() {
    super.initState();
    context.read<{{.PascalName}}{{.BlocSuffix}}>().add(const {{.PascalName}}Started());
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('{{.PascalName}}')),
      body: BlocBuilder<{{.PascalName}}{{.BlocSuffix}}, {{.PascalName}}{{.StateSuffix}}>(
        builder: (context, state) {
          return switch (state) {
            {{.PascalName}}Initial() => const SizedBox.shrink(),
            {{.PascalName}}Loading() => const Center(child: CircularProgressIndicator()),
            {{.PascalName}}Loaded() => const Center(child: Text('{{.PascalName}}')),
            {{.PascalName}}Error(:final message) => Center(child: Text(message)),
          };
        },
      ),
    );
  }
}
